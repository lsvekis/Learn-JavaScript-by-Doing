// Exercise 51: Basic Object Destructuring

Use { } syntax to match property names.
let user = { name: "Alice", age: 25 };
let { name, age } = user;
console.log(name, age);
{ name, age } destructures the corresponding properties from user.