// Exercise 23: Embedded Expressions

What You’ll Learn: Evaluate expressions inside ${}.
Learning Objectives:
let a = 5;
let b = 3;
let message = `The sum of a and b is ${a + b}`;
console.log(message);
${a + b} computes the sum inline.
Any valid JS expression can appear inside ${}.