// Exercise 32: Assignment Operators

What You’ll Learn: Using +=, -=, *=, /=, etc.
Learning Objectives:
let num = 10;
num += 5;  // num = num + 5
console.log(num);
num *= 2;  // num = num * 2
console.log(num);
Assignment operators combine arithmetic with assignment, e.g. num += 5 is num = num + 5.