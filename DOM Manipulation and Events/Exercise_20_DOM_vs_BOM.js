// Exercise 20: DOM vs. BOM

Distinguish document (DOM) from window, history, location (BOM).
console.log(window.location);   // BOM
console.log(document.body);     // DOM
The DOM deals with page structure and elements; the BOM includes browser-specific objects like location, history.