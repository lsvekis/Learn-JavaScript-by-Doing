// 27. Finding an Index with findIndex()

27. Finding an Index with findIndex()
let arr = ["alpha", "beta", "gamma"];
let idx = arr.findIndex(item => item === "beta");
console.log(idx); // 1
findIndex() returns the index or -1 if not found.