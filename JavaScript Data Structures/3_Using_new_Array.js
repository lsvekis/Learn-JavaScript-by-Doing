// 3. Using new Array()

3. Using new Array()
let arr = new Array(3); // Creates an array with length 3, all empty slots
console.log(arr, arr.length);
new Array(3) creates an array with 3 empty slots (not the same as [undefined, undefined, undefined]).
Generally, the literal form ([]) is preferred for clarity.