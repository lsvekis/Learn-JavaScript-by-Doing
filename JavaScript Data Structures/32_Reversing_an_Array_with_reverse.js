// 32. Reversing an Array with reverse()

32. Reversing an Array with reverse()
let arr = ["one", "two", "three"];
arr.reverse();
console.log(arr);
reverse() flips the array order in place, so the original array is changed.