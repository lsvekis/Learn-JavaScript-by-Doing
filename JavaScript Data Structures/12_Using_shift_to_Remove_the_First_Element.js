// 12. Using shift() to Remove the First Element

12. Using shift() to Remove the First Element
Compare shift() to pop().
let arr = [10, 20, 30];
let firstElement = arr.shift();
console.log(arr);
console.log("Removed:", firstElement);
shift() removes and returns the first element, shifting remaining elements left.